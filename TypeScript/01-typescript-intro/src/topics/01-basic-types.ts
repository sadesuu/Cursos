



//Variable constante(Es inmutable)
const name: string  = 'Hugo';
//Variable let(permite cambiar el valor) más operador OR(|)
let hpPoint: number | string = 100;

//Constante booleana
const isAlive: boolean = true;
hpPoint = 'FULL'

console.log({name, hpPoint, isAlive})
export{};